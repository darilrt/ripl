use crate::ast::Ast;

pub struct Interpreter {}

impl Interpreter {
    pub fn new() -> Self {
        Interpreter {}
    }

    pub fn exec(&self, _ast: Ast) {
        // Implement interpreter here
    }
}
